import { uiModuleAuthentication } from './ui-module-authentication';

describe('uiModuleAuthentication', () => {
  it('should work', () => {
    expect(uiModuleAuthentication()).toEqual('ui-module-authentication');
  });
});
