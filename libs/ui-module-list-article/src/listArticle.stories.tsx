import type { Meta } from '@storybook/react';
import { ListArticle } from './listArticle';

const Story: Meta<typeof ListArticle> = {
  component: ListArticle,
  title: 'ListArticle',
};
export default Story;

export const Primary = {
  args: {},
};
