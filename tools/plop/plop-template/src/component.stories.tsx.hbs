import type { Meta } from '@storybook/react';
import { {{sentenceCase name}} } from './{{kebabCase name}}'

const Story: Meta<typeof {{sentenceCase name}}> = {
  component: {{sentenceCase name}},
  title: '{{sentenceCase name}}',
};
export default Story;

export const Primary = {
  args: {},
};
